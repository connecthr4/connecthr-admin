version: "3.9"

services:
  connecthr-admin:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: connecthr-admin

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next

    environment:
      - NODE_ENV=development
