/**
* @module app/{{routeName}}/loading
*/

/**
* Loading component for the {{routeName}} route.
*
* Displays a loading state while the route
* or its data is being loaded.
* 
* @returns The loading UI as a React element.
*/
export default function Loading() {
  // TODO: Replace with a skeleton loader component
  return <p>Loading...</p>
}