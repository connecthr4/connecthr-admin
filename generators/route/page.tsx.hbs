/**
* @module app/{{routeName}}/page
*/

/**
* {{routeDescription}}
* 
* @remarks
* This page is responsible for rendering the UI
* for the `{{routeName}}` route.
*
* @returns The page UI for the route.
*/
export default function Layout() {
  return (
    <div>
      <h1>{{routeName}} page</h1>
    </div>
  )
}